version: '3.1'

services:

  wordpress_multisite:
    build: https://github.com/AndreaDiPietro-ADP/wordpress-dockerfile-development-purpose.git#docker-compose
    restart: "no"
    ports:
      - 8081:80
    environment:
      WORDPRESS_DB_HOST: wordpress_multisite_db
      WORDPRESS_DB_USER: exampleuser
      WORDPRESS_DB_PASSWORD: examplepass
      WORDPRESS_DB_NAME: exampledb
      XDEBUG_CONFIG: |
        xdebug.mode=debug,develop
        xdebug.client_host=host.docker.internal
        xdebug.client_port=9003
        xdebug.idekey="phpdebugkey"
      WORDPRESS_DEBUG: 1
      WORDPRESS_CONFIG_EXTRA: |
        /* Multisite */
        define('WP_ALLOW_MULTISITE', true );
    volumes:
      - ./shared_fs/wp_fs:/var/www/html
      
  wordpress_multisite_db:
    image: mysql:5.7
    restart: "no"
    environment:
      MYSQL_DATABASE: exampledb
      MYSQL_USER: exampleuser
      MYSQL_PASSWORD: examplepass
      MYSQL_RANDOM_ROOT_PASSWORD: '1'
    volumes:
      - ./shared_fs/wp_db:/var/lib/mysql
